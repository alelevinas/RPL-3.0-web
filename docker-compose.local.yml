services:
  local_front:
    build:
      context: .
      dockerfile: dev.Dockerfile
    ports:
      - "3000:8088"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    env_file:
      - .env.development
    stdin_open: true
    tty: true
  
